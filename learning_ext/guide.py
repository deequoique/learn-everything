"""软件使用指南 (Markdown 格式，显示在使用指南 Tab)"""

GUIDE_MARKDOWN = """
<div style="background:linear-gradient(135deg,#4F46E5,#818CF8);color:white;padding:24px;border-radius:12px;margin-bottom:24px;">
<h1 style="color:white;border:none;margin:0;">🎓 学习 Agent 使用指南</h1>
<p style="margin:8px 0 0;opacity:0.9;">给任意选题，AI 帮你制定路线、辅助搭建环境、记忆巩固、查漏补缺</p>
</div>

## ⚡ 30 秒快速上手

1. **配置 AI 模型**（首次必做）：打开 **🔧 模型配置** Tab → 填入你的 API Key
2. **创建学习路线**：打开 **🎯 学习路线** Tab → 输入选题（如"从零学 Python"）→ 点击「生成路线」
3. **保存项目**：路线满意后，点击「保存路线到项目」
4. **开始学习**：跟着路线上的知识点逐个推进，学完做复习和测验

---

## 📑 各 Tab 功能说明

### 📖 使用指南（当前页）
就是你现在看的这个页面，随时可以回来查。

### 💬 知识问答
这是基于知识库的 AI 问答（RAG）。
- **用法**：先在「📁 资料库」上传 PDF / 文档 / 笔记，然后在这里提问，AI 会基于你的资料回答，并标注引用出处（可点击跳转到原文位置）。
- **适合**：啃论文、查教材、问"这段代码什么意思"。

### 🎯 学习路线（核心功能）
输入任意选题，AI 自动拆解成**带依赖关系的分阶段学习路线**。
- **输入**：选题（学什么）+ 背景（你已会什么）+ 目标（学到什么程度）+ 每周可投入时间
- **输出**：知识图谱（DAG），分基础/强化/冲刺三阶段，每个知识点带预估学时、难度、前置依赖
- **可调整**：在「调整意见」框用自然语言让 AI 改路线（如"增加实战项目""去掉数学部分"）
- **保存**：点击「保存路线到项目」后，知识点会进入跟踪系统

### 🔄 间隔复习
基于 **FSRS v6 算法**（比 Anki 经典算法先进 30%）的艾宾浩斯记忆曲线复习。
- AI 会从你学过的知识点自动提炼复习卡片
- 点击「加载下一张」拉取到期卡片，显示答案后用 4 档评分（重来/困难/良好/简单）
- 算法根据评分自动安排下次复习时间，难的多复、易的少复
- **建议**：每天打开做一次，清空当天的待复习队列

### 📝 查漏测验
AI 按你的**薄弱知识点**自动出题，支持选择/填空/简答/实操四种题型。
- 做完后 AI 自动批改并给出解析
- 答错的知识点掌握度会下降，下次复习和测验会重点针对
- **建议**：每学完一个阶段，做一轮测验检查掌握情况

### 📊 学习看板
可视化你的学习进度。
- 知识点总数 / 已掌握 / 平均掌握度
- 学习热力图（最近 90 天每天学了多久，类似 GitHub 贡献图）
- AI 生成的学习日报

### 📁 资料库
上传和管理你的学习资料（PDF / Word / Markdown / 图片等）。
- AI 会自动解析、分块、建立向量索引
- 上传后即可在「💬 知识问答」里针对这些资料提问
- 支持多种索引模式：普通文件检索 / GraphRAG 知识图谱检索

### 🔧 模型配置
配置 AI 模型（**首次使用必须配置**）。支持的模型：
- **DeepSeek**（推荐，便宜好用）：`api.deepseek.com`，模型 `deepseek-chat`
- **智谱 GLM**：`open.bigmodel.cn`，模型 `glm-4-flash`（免费额度）
- **通义千问**：`dashscope.aliyuncs.com`，模型 `qwen-plus`
- **OpenAI**：`api.openai.com`，模型 `gpt-4o-mini`
- **本地 Ollama**：需先在本机跑 `ollama serve`

> 💡 配置时需要填：**接口地址**、**API Key**、**对话模型名**、**向量模型名**（用于资料检索）。
> 不用向量检索只做对话的话，向量模型可以随便填一个，但建议配上以获得完整体验。

### ⚙️ 设置
应用的偏好设置（推理模式、语言、上下文长度等）。一般保持默认即可。

### ❓ 帮助
Kotaemon 底座自带的帮助文档。

---

## 🗺️ 推荐学习流程

```
   ┌─────────────────────────────────────────────────┐
   │  1. 🔧 配模型  →  2. 🎯 生路线  →  3. 📁 传资料  │
   └─────────────────────────────────────────────────┘
                              │
                              ▼
   ┌─────────────────────────────────────────────────┐
   │           4. 按路线逐个知识点学习                │
   │  ┌─────────────────────────────────────────┐    │
   │  │  💬 不懂就问 RAG（查你上传的资料）       │    │
   │  └─────────────────────────────────────────┘    │
   └─────────────────────────────────────────────────┘
                              │
            ┌─────────────────┼─────────────────┐
            ▼                 ▼                 ▼
   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
   │ 5. 🔄 间隔复习 │  │ 6. 📝 查漏测验 │  │ 7. 📊 看进度  │
   │   (每天)      │  │   (每阶段)    │  │   (随时)      │
   └──────────────┘  └──────────────┘  └──────────────┘
```

---

## ❓ 常见问题

**Q：生成学习路线时报错怎么办？**
A：99% 是 AI 模型没配好。请先去 **🔧 模型配置** Tab 确认 API Key 有效、模型名正确。可在「💬 知识问答」Tab 发一句"你好"测试模型是否通。

**Q：复习/测验 Tab 显示"暂无数据"？**
A：这两个功能依赖学习路线里的知识点。请先在 **🎯 学习路线** 生成并保存一条路线，知识点会自动流入复习和测验系统。

**Q：上传的资料存在哪里？**
A：存在程序的 `kotaemon\\ktem_app_data\\` 目录下。卸载程序会丢失数据，迁移请备份这个目录。

**Q：支持哪些格式的资料？**
A：PDF、Word（doc/docx）、PPT、Excel、Markdown、TXT、HTML、图片（PNG/JPG/TIFF）、ZIP 压缩包。

**Q：可以同时学多个主题吗？**
A：可以。每个选题保存为一个独立的「学习项目」，互不干扰。

**Q：AI 回答不准确？**
A：尝试：① 在「🔧 模型配置」换更强的模型（如 `gpt-4o`、`deepseek-chat`）；② 调高「⚙️ 设置」里的上下文长度；③ 上传更准确的资料并在问答时关联对应资料库。

**Q：数据安全吗？**
A：完全本地存储，你的笔记、资料、学习记录都在本机，不会上传。只有调用 AI 时会把问题发给模型提供商。

---

## 💡 使用小技巧

- **路线不是死的**：随时可以在「调整意见」里让 AI 重排路线，学习本就是动态过程
- **费曼学习法**：学完一个知识点，试着不看资料向「💬 知识问答」解释一遍，AI 会扮演小白追问你
- **错题价值高**：测验做错的题会自动降低对应知识点的掌握度，让复习更聚焦薄弱环节
- **坚持比强度重要**：每天复习 10 分钟，比一周突击 3 小时效果好得多（这是间隔重复算法的核心原理）

---

<p style="text-align:center;color:#9CA3AF;margin-top:32px;">学习 Agent · 基于 Kotaemon + FSRS v6 · 祝你学有所成 🚀</p>
"""
